import { PDFSlideData } from '@/components/ui/dia/types'

/**
 * PDF Slide
 */

export const PDFSlide: React.FC<{ data: PDFSlideData }> = props => {
  const { data } = props

  return <div>PDF</div>
}
